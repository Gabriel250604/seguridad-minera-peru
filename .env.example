# Conexión a PostgreSQL
# Copia este archivo como .env y completa los valores con tus credenciales locales.
# El archivo .env NUNCA se sube al repositorio (está en .gitignore).
DB_HOST=localhost
DB_PORT=5432
DB_NAME=seguridad_minera
DB_USER=postgres
DB_PASSWORD=
